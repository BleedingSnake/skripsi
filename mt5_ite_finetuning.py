{"metadata":{"kernelspec":{"name":"python3","display_name":"Python 3","language":"python"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"accelerator":"GPU","colab":{"gpuType":"T4","name":"mT5_ITE_Law_Finetuning.ipynb","provenance":[]},"kaggle":{"accelerator":"gpu","dataSources":[{"sourceType":"datasetVersion","sourceId":14989697,"datasetId":9595063,"databundleVersionId":15863764}],"dockerImageVersionId":31287,"isInternetEnabled":true,"language":"python","sourceType":"script","isGpuEnabled":true}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"# %% [markdown]\n# # mT5-Small Fine-Tuning — ITE Law Chatbot\n# \n# This notebook fine-tunes `google/mt5-small` on a RAG-style text-to-text task.\n# The model learns to turn **(question + retrieved legal context) → natural Indonesian answer**.\n# \n# ---\n# ### Prerequisites\n# - Set **Runtime → Change runtime type → T4 GPU** before running\n# - Upload both dataset files to Colab (or mount Google Drive — see Cell 3)\n#   - `results_with_gold_v3.jsonl` — original 352 gold QA pairs\n#   - `gold_qa_generated_1500.jsonl` — generated 1500 QA pairs\n# \n# ### Pipeline overview\n# ```\n# User question + retrieved pasal/penjelasan text\n#         ↓  (packed into mT5 input)\n#   mT5-small (fine-tuned)\n#         ↓\n#   Natural Indonesian answer\n# ```\n\n# %% [markdown]\n# ## Cell 1 — Install dependencies\n# Run once per Colab session.\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T12:38:45.200386Z\",\"iopub.execute_input\":\"2026-02-28T12:38:45.201065Z\",\"iopub.status.idle\":\"2026-02-28T12:38:57.976239Z\",\"shell.execute_reply.started\":\"2026-02-28T12:38:45.201025Z\",\"shell.execute_reply\":\"2026-02-28T12:38:57.975577Z\"}}\n# ── Install dependencies ──────────────────────────────────────────────────────\nimport importlib, torch\n\nprint(\"transformers:\", importlib.import_module('transformers').__version__)\nprint(\"datasets    :\", importlib.import_module('datasets').__version__)\nprint(\"torch       :\", importlib.import_module('torch').__version__)\n\nprint(\"\\nGPU available:\", torch.cuda.is_available())\nif torch.cuda.is_available():\n    print(\"GPU:\", torch.cuda.get_device_name(0))\n    print(\"VRAM:\", round(torch.cuda.get_device_properties(0).total_memory / 1e9, 1), \"GB\")\n\n# %% [markdown]\n# ## Cell 2 — Configuration\n# All tunable settings live here. Adjust based on your GPU memory.\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T12:39:01.179941Z\",\"iopub.execute_input\":\"2026-02-28T12:39:01.180445Z\",\"iopub.status.idle\":\"2026-02-28T12:39:01.187110Z\",\"shell.execute_reply.started\":\"2026-02-28T12:39:01.180414Z\",\"shell.execute_reply\":\"2026-02-28T12:39:01.186505Z\"}}\n# ── Configuration ─────────────────────────────────────────────────────────────\nimport os\n# Model\nMODEL_NAME        = \"google/mt5-small\"   # ~300M params, fits T4 (16GB)\n                                          # Use \"google/mt5-base\" if you have A100\n\n# Paths — adjust if using Google Drive (see Cell 3)\nBASE_PATH = \"/kaggle/input/datasets/anomwibisono/one-shall-stand-one-shall-fall\"\n\nGOLD_ORIG_PATH = os.path.join(BASE_PATH, \"results_with_gold_v3.jsonl\")\nGOLD_GEN_PATH  = os.path.join(BASE_PATH, \"gold_qa_generated_1500.jsonl\")\nOUTPUT_DIR     = \"/kaggle/working/mt5_ite_model\"\nBEST_MODEL_DIR = \"/kaggle/working/mt5_ite_best\"\n\n# Tokenization — mT5-small max is 512 for input, 256 for output\nMAX_INPUT_LEN     = 512   # question + context\nMAX_TARGET_LEN    = 256   # gold answer\n\n# Training\nBATCH_SIZE        = 2     # reduce to 4 if OOM on T4\nGRAD_ACCUM        = 16     # effective batch = BATCH_SIZE * GRAD_ACCUM = 32\nEPOCHS            = 5\nLR                = 5e-4\nWARMUP_RATIO      = 0.1\nWEIGHT_DECAY      = 0.01\nVAL_SPLIT         = 0.1   # 10% of data held out for validation\n\n# Reproducibility\nSEED              = 42\n\nprint(\"Config loaded.\")\nprint(f\"  Model:        {MODEL_NAME}\")\nprint(f\"  Max input:    {MAX_INPUT_LEN} tokens\")\nprint(f\"  Max output:   {MAX_TARGET_LEN} tokens\")\nprint(f\"  Batch size:   {BATCH_SIZE} × {GRAD_ACCUM} accum = {BATCH_SIZE*GRAD_ACCUM} effective\")\nprint(f\"  Epochs:       {EPOCHS}\")\nprint(f\"  Learning rate:{LR}\")\n\n# %% [markdown]\n# ## Cell 3 — (Optional) Mount Google Drive\n# Skip this cell if you uploaded files directly. Use it if your datasets are in Drive.\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:19:30.773088Z\",\"iopub.execute_input\":\"2026-02-28T10:19:30.773335Z\",\"iopub.status.idle\":\"2026-02-28T10:19:30.787046Z\",\"shell.execute_reply.started\":\"2026-02-28T10:19:30.773311Z\",\"shell.execute_reply\":\"2026-02-28T10:19:30.786248Z\"}}\n# ── OPTIONAL: Mount Google Drive ─────────────────────────────────────────────\n# Uncomment if your files are stored in Drive\n\n# from google.colab import drive\n# drive.mount('/content/drive')\n\n# # Then update paths above to something like:\n# GOLD_ORIG_PATH = \"/content/drive/MyDrive/ITE_chatbot/results_with_gold_v3.jsonl\"\n# GOLD_GEN_PATH  = \"/content/drive/MyDrive/ITE_chatbot/gold_qa_generated_1500.jsonl\"\n# OUTPUT_DIR     = \"/content/drive/MyDrive/ITE_chatbot/mt5_ite_model\"\n# BEST_MODEL_DIR = \"/content/drive/MyDrive/ITE_chatbot/mt5_ite_best\"\n\n#print(\"Skipped (using local /content uploads).\")\n#print(\"To use Drive, uncomment the lines above.\")\n\n# %% [markdown]\n# ## Cell 4 — Load and inspect datasets\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:19:30.787930Z\",\"iopub.execute_input\":\"2026-02-28T10:19:30.788150Z\",\"iopub.status.idle\":\"2026-02-28T10:19:30.839904Z\",\"shell.execute_reply.started\":\"2026-02-28T10:19:30.788132Z\",\"shell.execute_reply\":\"2026-02-28T10:19:30.839169Z\"}}\n# ── Load raw JSONL datasets ───────────────────────────────────────────────────\nimport json\nfrom collections import defaultdict\n\n\ndef load_jsonl(path):\n    records = []\n    with open(path, encoding=\"utf-8\") as f:\n        for line in f:\n            line = line.strip()\n            if line:\n                records.append(json.loads(line))\n    return records\n\ngold_orig = load_jsonl(GOLD_ORIG_PATH)\ngold_gen  = load_jsonl(GOLD_GEN_PATH)\nall_data  = gold_orig + gold_gen\n\nprint(f\"Original gold entries : {len(gold_orig)}\")\nprint(f\"Generated entries     : {len(gold_gen)}\")\nprint(f\"Total                 : {len(all_data)}\")\n\n# Intent distribution\ndist = defaultdict(int)\nfor d in all_data:\n    dist[d['detected_intent']] += 1\nprint(\"\\nIntent distribution:\")\nfor intent, count in sorted(dist.items()):\n    print(f\"  {intent:<20} {count}\")\n\n# Spot-check one entry\nprint(\"\\n── Sample entry ─────────────────────────────────\")\nsample = all_data[0]\nprint(\"Question:\", sample['question'])\nprint(\"Intent  :\", sample['detected_intent'])\nprint(\"Answer  :\", sample['gold_answer'][:200], \"...\")\n\n# %% [markdown]\n# ## Cell 5 — Build input/output text pairs\n# Each example becomes:\n# - **Input:** `jawab pertanyaan: {question} konteks: {top retrieved text}`\n# - **Target:** `{gold_answer}`\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:19:30.871918Z\",\"iopub.execute_input\":\"2026-02-28T10:19:30.872575Z\",\"iopub.status.idle\":\"2026-02-28T10:19:30.909785Z\",\"shell.execute_reply.started\":\"2026-02-28T10:19:30.872542Z\",\"shell.execute_reply\":\"2026-02-28T10:19:30.909299Z\"}}\n# ── Build seq2seq text pairs ──────────────────────────────────────────────────\n\ndef build_input_text(record):\n    \"\"\"\n    Constructs the mT5 input string from a JSONL record.\n    Uses the top-1 pasal result + top-1 penjelasan result (if available).\n    \"\"\"\n    question = record['question']\n\n    # Top pasal chunk\n    pasal_text = \"\"\n    if record.get('pasal_results'):\n        pasal_text = record['pasal_results'][0].get('text', '')\n\n    # Top penjelasan chunk (optional enrichment)\n    penj_text = \"\"\n    if record.get('penjelasan_results'):\n        penj_text = record['penjelasan_results'][0].get('text', '')\n\n    # Combine context — penjelasan is secondary, truncation handles overflow\n    if penj_text:\n        context = f\"{pasal_text} [PENJELASAN] {penj_text}\"\n    else:\n        context = pasal_text\n\n    return f\"jawab pertanyaan: {question} konteks: {context}\"\n\n\n# Build paired lists\ninput_texts  = []\ntarget_texts = []\nskipped      = 0\n\nfor record in all_data:\n    answer = record.get('gold_answer', '').strip()\n    if not answer or answer == \"Aku tidak menemukan pasal yang benar-benar relevan berdasarkan hasil pencarian.\":\n        skipped += 1\n        continue\n    input_texts.append(build_input_text(record))\n    target_texts.append(answer)\n\nprint(f\"Valid pairs : {len(input_texts)}\")\nprint(f\"Skipped     : {skipped} (no-result entries)\")\n\n# Length statistics\nimport numpy as np\nin_lens  = [len(t.split()) for t in input_texts]\nout_lens = [len(t.split()) for t in target_texts]\nprint(f\"\\nInput  word-length  — mean: {np.mean(in_lens):.0f}, p95: {np.percentile(in_lens, 95):.0f}, max: {max(in_lens)}\")\nprint(f\"Output word-length  — mean: {np.mean(out_lens):.0f}, p95: {np.percentile(out_lens, 95):.0f}, max: {max(out_lens)}\")\n\nprint(\"\\n── Sample input text ────────────────────────────\")\nprint(input_texts[0][:400], \"...\")\nprint(\"\\n── Sample target text ───────────────────────────\")\nprint(target_texts[0][:300], \"...\")\n\n# %% [markdown]\n# ## Cell 6 — Tokenize and create HuggingFace Dataset\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:19:30.911616Z\",\"iopub.execute_input\":\"2026-02-28T10:19:30.911939Z\",\"iopub.status.idle\":\"2026-02-28T10:19:36.514442Z\",\"shell.execute_reply.started\":\"2026-02-28T10:19:30.911902Z\",\"shell.execute_reply\":\"2026-02-28T10:19:36.513656Z\"}}\n# ── Tokenize ──────────────────────────────────────────────────────────────────\nimport random\nimport numpy as np\nimport torch\nfrom transformers import AutoTokenizer\nfrom datasets import Dataset\n\n# Seed everything for reproducibility\nrandom.seed(SEED)\nnp.random.seed(SEED)\ntorch.manual_seed(SEED)\n\nprint(\"Loading tokenizer...\")\ntokenizer = AutoTokenizer.from_pretrained(MODEL_NAME)\nprint(\"Tokenizer vocab size:\", tokenizer.vocab_size)\n\ndef preprocess(batch):\n    \"\"\"\n    Tokenizes inputs and targets.\n    Labels have -100 on padding positions so they're ignored in cross-entropy loss.\n    \"\"\"\n    model_inputs = tokenizer(\n        batch[\"input_text\"],\n        max_length=MAX_INPUT_LEN,\n        truncation=True,\n        padding=\"max_length\",\n    )\n\n    labels = tokenizer(\n        batch[\"target_text\"],\n        max_length=MAX_TARGET_LEN,\n        truncation=True,\n        padding=\"max_length\",\n    )\n\n    # Replace padding token id with -100 so it doesn't contribute to loss\n    label_ids = [\n        [(l if l != tokenizer.pad_token_id else -100) for l in label]\n        for label in labels[\"input_ids\"]\n    ]\n    model_inputs[\"labels\"] = label_ids\n    return model_inputs\n\n\n# Build HuggingFace Dataset\nraw_dataset = Dataset.from_dict({\n    \"input_text\":  input_texts,\n    \"target_text\": target_texts,\n})\n\n# Shuffle then split into train / validation\nraw_dataset = raw_dataset.shuffle(seed=SEED)\nsplit       = raw_dataset.train_test_split(test_size=VAL_SPLIT, seed=SEED)\ntrain_raw   = split[\"train\"]\nval_raw     = split[\"test\"]\n\nprint(f\"Train size : {len(train_raw)}\")\nprint(f\"Val size   : {len(val_raw)}\")\n\n# Tokenize both splits\nprint(\"\\nTokenizing train split...\")\ntrain_dataset = train_raw.map(\n    preprocess, batched=True, batch_size=64,\n    remove_columns=[\"input_text\", \"target_text\"]\n)\nprint(\"Tokenizing validation split...\")\nval_dataset = val_raw.map(\n    preprocess, batched=True, batch_size=64,\n    remove_columns=[\"input_text\", \"target_text\"]\n)\n\ntrain_dataset.set_format(\"torch\")\nval_dataset.set_format(\"torch\")\nprint(\"\\nDatasets ready.\")\nprint(\"Train features:\", train_dataset.features)\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:19:36.515420Z\",\"iopub.execute_input\":\"2026-02-28T10:19:36.515676Z\",\"iopub.status.idle\":\"2026-02-28T10:19:36.782733Z\",\"shell.execute_reply.started\":\"2026-02-28T10:19:36.515640Z\",\"shell.execute_reply\":\"2026-02-28T10:19:36.781987Z\"}}\nimport torch, gc\n\n# Delete old model from GPU if it exists\ntry:\n    del model\nexcept:\n    pass\n\ngc.collect()\ntorch.cuda.empty_cache()\nprint(\"GPU memory freed\")\nprint(f\"Free VRAM: {torch.cuda.mem_get_info()[0] / 1e9:.1f} GB\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:19:36.783741Z\",\"iopub.execute_input\":\"2026-02-28T10:19:36.784065Z\",\"iopub.status.idle\":\"2026-02-28T10:19:36.996285Z\",\"shell.execute_reply.started\":\"2026-02-28T10:19:36.784028Z\",\"shell.execute_reply\":\"2026-02-28T10:19:36.995303Z\"}}\n!nvidia-smi\n\n# %% [markdown]\n# ## Cell 7 — Load model and set up training\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:19:36.999165Z\",\"iopub.execute_input\":\"2026-02-28T10:19:36.999535Z\",\"iopub.status.idle\":\"2026-02-28T10:19:45.302697Z\",\"shell.execute_reply.started\":\"2026-02-28T10:19:36.999506Z\",\"shell.execute_reply\":\"2026-02-28T10:19:45.299822Z\"}}\n# ── Load model ────────────────────────────────────────────────────────────────\nimport os\nos.environ[\"PYTORCH_CUDA_ALLOC_CONF\"] = \"expandable_segments:True\"\n\nfrom transformers import AutoModelForSeq2SeqLM, Seq2SeqTrainingArguments, Seq2SeqTrainer\nfrom transformers import DataCollatorForSeq2Seq\nimport os\n\nprint(\"Loading model...\")\nmodel = AutoModelForSeq2SeqLM.from_pretrained(MODEL_NAME)\n\n# Parameter count\ntotal_params     = sum(p.numel() for p in model.parameters())\ntrainable_params = sum(p.numel() for p in model.parameters() if p.requires_grad)\nprint(f\"Total params    : {total_params:,}\")\nprint(f\"Trainable params: {trainable_params:,}\")\n\n# Data collator handles dynamic padding during batching\ndata_collator = DataCollatorForSeq2Seq(\n    tokenizer,\n    model=model,\n    label_pad_token_id=-100,\n    pad_to_multiple_of=8,  # efficient on GPU tensor cores\n)\n\n# ── Training arguments ────────────────────────────────────────────────────────\ntraining_args = Seq2SeqTrainingArguments(\n    output_dir                  = OUTPUT_DIR,\n    num_train_epochs            = EPOCHS,\n    per_device_train_batch_size = BATCH_SIZE,\n    per_device_eval_batch_size  = BATCH_SIZE,\n    gradient_accumulation_steps = GRAD_ACCUM,\n    learning_rate               = LR,\n    warmup_ratio                = WARMUP_RATIO,\n    weight_decay                = WEIGHT_DECAY,\n    predict_with_generate       = True,  # required for seq2seq eval\n    generation_max_length       = MAX_TARGET_LEN,\n    eval_strategy               = \"epoch\",\n    save_strategy               = \"epoch\",\n    load_best_model_at_end      = True,\n    metric_for_best_model       = \"rouge2\",   # we'll define this below\n    greater_is_better           = True,\n    fp16  = False,\n    bf16  = False,  # mixed precision on GPU\n    logging_steps               = 50,\n    save_total_limit            = 5,          # keep only 2 checkpoints\n    seed                        = SEED,\n    report_to                   = \"none\",     # set to \"wandb\" if you want W&B logging\n)\n\nprint(\"\\nTraining arguments set.\")\nprint(f\"  Effective batch size : {BATCH_SIZE * GRAD_ACCUM}\")\nprint(f\"  fp16 (mixed prec.)   : {training_args.fp16}\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:19:45.304272Z\",\"iopub.execute_input\":\"2026-02-28T10:19:45.306821Z\",\"iopub.status.idle\":\"2026-02-28T10:19:45.326386Z\",\"shell.execute_reply.started\":\"2026-02-28T10:19:45.306782Z\",\"shell.execute_reply\":\"2026-02-28T10:19:45.312955Z\"}}\nmodel.config.tie_word_embeddings = False\n\n# %% [markdown]\n# ## Cell 8 — Define evaluation metric (ROUGE)\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:19:45.327853Z\",\"iopub.execute_input\":\"2026-02-28T10:19:45.328544Z\",\"iopub.status.idle\":\"2026-02-28T10:19:46.349268Z\",\"shell.execute_reply.started\":\"2026-02-28T10:19:45.328457Z\",\"shell.execute_reply\":\"2026-02-28T10:19:46.346632Z\"}}\n# ── ROUGE metric for seq2seq evaluation ──────────────────────────────────────\nimport nltk\nimport numpy as np\nfrom rouge_score import rouge_scorer\n\nnltk.download('punkt', quiet=True)\n\ndef compute_metrics(eval_preds):\n    preds, labels = eval_preds\n\n    if isinstance(preds, tuple):\n        preds = preds[0]\n\n    # Fix for transformers 5.x — clip to valid token id range\n    preds = np.clip(preds, 0, tokenizer.vocab_size - 1).astype(np.int32)\n\n    decoded_preds = tokenizer.batch_decode(preds, skip_special_tokens=True)\n    labels = np.where(labels != -100, labels, tokenizer.pad_token_id)\n    decoded_labels = tokenizer.batch_decode(labels, skip_special_tokens=True)\n\n    decoded_preds  = [p.strip() for p in decoded_preds]\n    decoded_labels = [l.strip() for l in decoded_labels]\n\n    scorer = rouge_scorer.RougeScorer(['rouge1', 'rouge2', 'rougeL'], use_stemmer=False)\n\n    r1, r2, rl = [], [], []\n    for pred, label in zip(decoded_preds, decoded_labels):\n        scores = scorer.score(label, pred)\n        r1.append(scores['rouge1'].fmeasure)\n        r2.append(scores['rouge2'].fmeasure)\n        rl.append(scores['rougeL'].fmeasure)\n\n    return {\n        \"rouge1\": round(np.mean(r1), 4),\n        \"rouge2\": round(np.mean(r2), 4),\n        \"rougeL\": round(np.mean(rl), 4),\n    }\n\nprint(\"Metric function defined.\")\nprint(\"Monitoring: rouge1, rouge2, rougeL\")\nprint(\"Best model selected by: rouge2\")\n\n# %% [markdown]\n# ## Cell 9 — Train!\n# \n# Expected runtime on T4:\n# - ~1852 train examples, batch 32 effective → ~58 steps/epoch\n# - 5 epochs → ~290 steps total\n# - **Roughly 20–35 minutes** on T4\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:19:46.350325Z\",\"iopub.execute_input\":\"2026-02-28T10:19:46.351395Z\",\"iopub.status.idle\":\"2026-02-28T10:52:36.429291Z\",\"shell.execute_reply.started\":\"2026-02-28T10:19:46.351352Z\",\"shell.execute_reply\":\"2026-02-28T10:52:36.428540Z\"}}\n# ── Initialize Trainer and start training ────────────────────────────────────\nimport time\n\ntrainer = Seq2SeqTrainer(\n    model           = model,\n    args            = training_args,\n    train_dataset   = train_dataset,\n    eval_dataset    = val_dataset,\n    processing_class       = tokenizer,\n    data_collator   = data_collator,\n    compute_metrics = compute_metrics,\n)\n\nprint(\"Starting training...\")\nprint(f\"Train samples   : {len(train_dataset)}\")\nprint(f\"Val samples     : {len(val_dataset)}\")\nprint(f\"Epochs          : {EPOCHS}\")\nprint(f\"Steps per epoch : {len(train_dataset) // (BATCH_SIZE * GRAD_ACCUM)}\")\nprint()\n\nstart_time = time.time()\ntrain_result = trainer.train()\nelapsed = time.time() - start_time\n\nprint(f\"\\nTraining complete in {elapsed/60:.1f} minutes\")\nprint(\"Train loss:\", round(train_result.training_loss, 4))\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:52:36.430359Z\",\"iopub.execute_input\":\"2026-02-28T10:52:36.431011Z\",\"iopub.status.idle\":\"2026-02-28T10:52:36.437224Z\",\"shell.execute_reply.started\":\"2026-02-28T10:52:36.430974Z\",\"shell.execute_reply\":\"2026-02-28T10:52:36.436647Z\"}}\n# Check a sample from train_dataset\nsample = train_dataset[0]\nlabels = sample['labels'].tolist()\n\nnon_masked = [l for l in labels if l != -100]\nprint(f\"Total label tokens : {len(labels)}\")\nprint(f\"Non-masked (-100)  : {len(non_masked)}\")\nprint(f\"All masked?        : {len(non_masked) == 0}\")\nprint(f\"Sample non-masked tokens: {non_masked[:20]}\")\nprint(f\"Decoded: {tokenizer.decode(non_masked)}\")\n\n# %% [markdown]\n# ## Cell 10 — Save best model\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T12:39:12.562584Z\",\"iopub.execute_input\":\"2026-02-28T12:39:12.563413Z\",\"iopub.status.idle\":\"2026-02-28T12:39:12.570948Z\",\"shell.execute_reply.started\":\"2026-02-28T12:39:12.563382Z\",\"shell.execute_reply\":\"2026-02-28T12:39:12.569732Z\"}}\n# ── Save the best checkpoint ──────────────────────────────────────────────────\nimport os\n\nos.makedirs(BEST_MODEL_DIR, exist_ok=True)\ntrainer.save_model(BEST_MODEL_DIR)\ntokenizer.save_pretrained(BEST_MODEL_DIR)\n\nprint(f\"Best model saved to: {BEST_MODEL_DIR}\")\nprint(\"Files:\", os.listdir(BEST_MODEL_DIR))\n\n# Print final eval metrics\nprint(\"\\nFinal validation metrics:\")\neval_results = trainer.evaluate()\nfor k, v in eval_results.items():\n    print(f\"  {k}: {v}\")\n\n# %% [markdown]\n# ## Cell 11 — (Optional) Save to Google Drive\n# Keeps your model persistent after Colab disconnects.\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:57:10.271478Z\",\"iopub.execute_input\":\"2026-02-28T10:57:10.271748Z\",\"iopub.status.idle\":\"2026-02-28T10:57:10.276146Z\",\"shell.execute_reply.started\":\"2026-02-28T10:57:10.271726Z\",\"shell.execute_reply\":\"2026-02-28T10:57:10.275351Z\"}}\n# ── OPTIONAL: Copy model to Google Drive ─────────────────────────────────────\n# Uncomment to persist model across Colab sessions\n\n# from google.colab import drive\n# drive.mount('/content/drive')\n\n# import shutil\n# DRIVE_MODEL_PATH = \"/content/drive/MyDrive/ITE_chatbot/mt5_ite_best\"\n# shutil.copytree(BEST_MODEL_DIR, DRIVE_MODEL_PATH, dirs_exist_ok=True)\n# print(\"Model saved to Drive:\", DRIVE_MODEL_PATH)\n\nprint(\"Skipped. Uncomment lines above to save to Drive.\")\n\n# %% [markdown]\n# ## Cell 12 — Inference: generate answers\n# Load your fine-tuned model and test it with real questions.\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:57:10.277139Z\",\"iopub.execute_input\":\"2026-02-28T10:57:10.277427Z\",\"iopub.status.idle\":\"2026-02-28T10:57:13.676488Z\",\"shell.execute_reply.started\":\"2026-02-28T10:57:10.277371Z\",\"shell.execute_reply\":\"2026-02-28T10:57:13.675720Z\"}}\n# ── Inference helper ──────────────────────────────────────────────────────────\nfrom transformers import AutoTokenizer, AutoModelForSeq2SeqLM\nimport torch\n\nDEVICE = \"cuda\" if torch.cuda.is_available() else \"cpu\"\n\n# Load from best checkpoint\ninf_tokenizer = AutoTokenizer.from_pretrained(BEST_MODEL_DIR)\ninf_model     = AutoModelForSeq2SeqLM.from_pretrained(BEST_MODEL_DIR).to(DEVICE)\ninf_model.eval()\n\ndef generate_answer(question: str, context: str, max_new_tokens: int = 256) -> str:\n    \"\"\"\n    Given a question and retrieved legal context, generate a natural Indonesian answer.\n\n    Args:\n        question       : user's question\n        context        : top retrieved pasal text (from your hybrid retriever)\n        max_new_tokens : max answer length in tokens\n\n    Returns:\n        Generated answer string\n    \"\"\"\n    input_text = f\"jawab pertanyaan: {question} konteks: {context}\"\n    inputs = inf_tokenizer(\n        input_text,\n        return_tensors=\"pt\",\n        max_length=MAX_INPUT_LEN,\n        truncation=True,\n    ).to(DEVICE)\n\n    with torch.no_grad():\n        output_ids = inf_model.generate(\n            **inputs,\n            max_new_tokens   = max_new_tokens,\n            num_beams        = 4,      # beam search for better quality\n            early_stopping   = True,\n            no_repeat_ngram_size = 3,  # avoid repetitive output\n            length_penalty   = 1.0,\n        )\n\n    return inf_tokenizer.decode(output_ids[0], skip_special_tokens=True)\n\n\nprint(\"Inference function ready.\")\nprint(\"Usage:\")\nprint('  answer = generate_answer(question, context)')\nprint('  print(answer)')\n\n# %% [markdown]\n# ## Cell 13 — Run test inferences\n# These simulate what happens when your hybrid retriever feeds context to mT5.\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:57:13.677558Z\",\"iopub.execute_input\":\"2026-02-28T10:57:13.677986Z\",\"iopub.status.idle\":\"2026-02-28T10:57:19.668183Z\",\"shell.execute_reply.started\":\"2026-02-28T10:57:13.677952Z\",\"shell.execute_reply\":\"2026-02-28T10:57:19.667501Z\"}}\n# ── Test with real examples ───────────────────────────────────────────────────\n\ntest_cases = [\n    {\n        \"question\": \"Apa isi Pasal 27 ayat (1) UU ITE?\",\n        \"context\" : (\n            \"Setiap Orang dengan sengaja dan tanpa hak menyiarkan, mempertunjukkan, \"\n            \"mendistribusikan, mentransmisikan, dan/atau membuat dapat diaksesnya \"\n            \"Informasi Elektronik dan/atau Dokumen Elektronik yang memiliki muatan \"\n            \"yang melanggar kesusilaan untuk diketahui umum.\"\n        )\n    },\n    {\n        \"question\": \"Apa sanksi untuk penyebaran berita bohong?\",\n        \"context\" : (\n            \"Setiap Orang yang dengan sengaja mendistribusikan dan/atau mentransmisikan \"\n            \"Informasi Elektronik dan/atau Dokumen Elektronik yang berisi pemberitahuan \"\n            \"bohong atau informasi menyesatkan yang mengakibatkan kerugian materiel bagi \"\n            \"konsumen dalam Transaksi Elektronik dipidana dengan pidana penjara paling \"\n            \"lama 6 (enam) tahun dan/atau denda paling banyak Rp1.000.000.000,00.\"\n        )\n    },\n    {\n        \"question\": \"Apa yang dimaksud dengan Tanda Tangan Elektronik?\",\n        \"context\" : (\n            \"Tanda Tangan Elektronik adalah tanda tangan yang terdiri atas Informasi \"\n            \"Elektronik yang dilekatkan, terasosiasi atau terkait dengan Informasi \"\n            \"Elektronik lainnya yang digunakan sebagai alat verifikasi dan autentikasi.\"\n        )\n    },\n    {\n        \"question\": \"Apakah boleh menyebarkan data pribadi orang lain tanpa izin?\",\n        \"context\" : (\n            \"Setiap Orang dengan sengaja dan tanpa hak mendistribusikan dan/atau \"\n            \"mentransmisikan dan/atau membuat dapat diaksesnya Informasi Elektronik \"\n            \"dan/atau Dokumen Elektronik yang memiliki muatan penghinaan dan/atau \"\n            \"pencemaran nama baik.\"\n        )\n    },\n]\n\nfor i, tc in enumerate(test_cases, 1):\n    print(f\"{'='*60}\")\n    print(f\"Test {i}\")\n    print(f\"Q: {tc['question']}\")\n    answer = generate_answer(tc['question'], tc['context'])\n    print(f\"A: {answer}\")\n    print()\n\n# %% [markdown]\n# ## Cell 14 — Evaluate on held-out validation set\n# Computes ROUGE scores across the full validation split for a performance summary.\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:57:19.670199Z\",\"iopub.execute_input\":\"2026-02-28T10:57:19.670741Z\",\"iopub.status.idle\":\"2026-02-28T10:58:34.328360Z\",\"shell.execute_reply.started\":\"2026-02-28T10:57:19.670717Z\",\"shell.execute_reply\":\"2026-02-28T10:58:34.327447Z\"}}\n# ── Full validation evaluation ────────────────────────────────────────────────\nfrom rouge_score import rouge_scorer as rs_module\nimport numpy as np\n\nscorer = rs_module.RougeScorer(['rouge1', 'rouge2', 'rougeL'], use_stemmer=False)\n\n# Use the raw (pre-tokenized) val split for human-readable results\nval_inputs  = val_raw[\"input_text\"]\nval_targets = val_raw[\"target_text\"]\n\nprint(f\"Evaluating on {len(val_inputs)} validation examples...\")\n\nr1_scores, r2_scores, rl_scores = [], [], []\nEVAL_BATCH = 16\n\nfor i in range(0, len(val_inputs), EVAL_BATCH):\n    batch_inputs  = val_inputs[i:i+EVAL_BATCH]\n    batch_targets = val_targets[i:i+EVAL_BATCH]\n\n    encoded = inf_tokenizer(\n        batch_inputs,\n        max_length=MAX_INPUT_LEN,\n        truncation=True,\n        padding=True,\n        return_tensors=\"pt\"\n    ).to(DEVICE)\n\n    with torch.no_grad():\n        outputs = inf_model.generate(\n            **encoded,\n            max_new_tokens=MAX_TARGET_LEN,\n            num_beams=4,\n            early_stopping=True,\n            no_repeat_ngram_size=3,\n        )\n\n    decoded = inf_tokenizer.batch_decode(outputs, skip_special_tokens=True)\n\n    for pred, target in zip(decoded, batch_targets):\n        s = scorer.score(target.strip(), pred.strip())\n        r1_scores.append(s['rouge1'].fmeasure)\n        r2_scores.append(s['rouge2'].fmeasure)\n        rl_scores.append(s['rougeL'].fmeasure)\n\n    if (i // EVAL_BATCH) % 5 == 0:\n        print(f\"  Evaluated {min(i+EVAL_BATCH, len(val_inputs))}/{len(val_inputs)}\")\n\nprint(\"\\n── Validation ROUGE Scores ──────────────────────\")\nprint(f\"  ROUGE-1 : {np.mean(r1_scores):.4f}\")\nprint(f\"  ROUGE-2 : {np.mean(r2_scores):.4f}\")\nprint(f\"  ROUGE-L : {np.mean(rl_scores):.4f}\")\nprint()\nprint(\"Interpretation guide:\")\nprint(\"  ROUGE-L > 0.40 → Good answer quality\")\nprint(\"  ROUGE-L > 0.50 → Excellent answer quality\")\nprint(\"  ROUGE-L < 0.25 → Consider more training epochs or data\")\n\n# %% [markdown]\n# ## Cell 15 — (Optional) Download model as zip\n# If not using Drive, zip and download the model directly.\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T10:58:34.329451Z\",\"iopub.execute_input\":\"2026-02-28T10:58:34.329754Z\",\"iopub.status.idle\":\"2026-02-28T10:58:34.334645Z\",\"shell.execute_reply.started\":\"2026-02-28T10:58:34.329718Z\",\"shell.execute_reply\":\"2026-02-28T10:58:34.333795Z\"}}\n# ── OPTIONAL: Zip and download model ─────────────────────────────────────────\n# Uncomment to download the model to your local machine\n\n# import shutil\n# from google.colab import files\n\n# zip_path = \"/content/mt5_ite_best.zip\"\n# shutil.make_archive(\"/content/mt5_ite_best\", 'zip', BEST_MODEL_DIR)\n# files.download(zip_path)\n# print(\"Download started.\")\n\nprint(\"Skipped. Uncomment lines above to download model as zip.\")\n\n# %% [markdown]\n# ---\n# ## Connecting mT5 to your existing hybrid retriever\n# \n# Once trained, integrating mT5 with your retriever is straightforward:\n# \n# ```python\n# # Pseudocode — slot into your intent_aware_retriever.py pipeline\n# \n# from transformers import AutoTokenizer, AutoModelForSeq2SeqLM\n# \n# # Load once at startup\n# generator_tokenizer = AutoTokenizer.from_pretrained(\"path/to/mt5_ite_best\")\n# generator_model     = AutoModelForSeq2SeqLM.from_pretrained(\"path/to/mt5_ite_best\")\n# \n# def answer_query(user_question):\n#     # Step 1: your existing hybrid retriever\n#     retrieval_results = hybrid_retriever.search(user_question)\n#     top_pasal_text    = retrieval_results['pasal_results'][0]['text']\n#     top_penj_text     = retrieval_results.get('penjelasan_results', [{}])[0].get('text', '')\n# \n#     # Step 2: build context\n#     if top_penj_text:\n#         context = f\"{top_pasal_text} [PENJELASAN] {top_penj_text}\"\n#     else:\n#         context = top_pasal_text\n# \n#     # Step 3: mT5 generates natural answer\n#     input_text = f\"jawab pertanyaan: {user_question} konteks: {context}\"\n#     inputs     = generator_tokenizer(input_text, return_tensors=\"pt\", truncation=True, max_length=512)\n#     outputs    = generator_model.generate(**inputs, max_new_tokens=256, num_beams=4)\n#     answer     = generator_tokenizer.decode(outputs[0], skip_special_tokens=True)\n# \n#     return answer\n# ```\n# \n# ---\n# ## Troubleshooting\n# \n# | Problem | Fix |\n# |---|---|\n# | CUDA out of memory | Reduce `BATCH_SIZE` to 4, keep `GRAD_ACCUM=8` |\n# | Loss stuck > 3.0 after epoch 2 | Lower LR to `3e-4` |\n# | Output is repetitive / loops | Increase `no_repeat_ngram_size` to 4 |\n# | Output too short | Increase `max_new_tokens` to 512, lower `length_penalty` |\n# | Training very slow | Enable fp16, or switch to mt5-small if using mt5-base |\n# | Session disconnects mid-training | Save to Drive (Cell 11), or use resuming: `trainer.train(resume_from_checkpoint=True)` |\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-02-28T12:39:47.407714Z\",\"iopub.execute_input\":\"2026-02-28T12:39:47.408023Z\",\"iopub.status.idle\":\"2026-02-28T12:39:47.414612Z\",\"shell.execute_reply.started\":\"2026-02-28T12:39:47.407997Z\",\"shell.execute_reply\":\"2026-02-28T12:39:47.413841Z\"}}\nimport os\n\noutput_dir = \"/kaggle/working/mt5_ite_model\"\nbest_dir = \"/kaggle/working/mt5_ite_best\"\n\nprint(\"=== OUTPUT DIR ===\")\nif os.path.exists(output_dir):\n    print(os.listdir(output_dir))\nelse:\n    print(\"NOT FOUND\")\n\nprint(\"\\n=== BEST DIR ===\")\nif os.path.exists(best_dir):\n    print(os.listdir(best_dir))\nelse:\n    print(\"NOT FOUND\")\n\nprint(\"\\n=== KAGGLE WORKING ROOT ===\")\nprint(os.listdir(\"/kaggle/working\"))","metadata":{"_uuid":"c5d2a730-8f45-4fd1-9311-2069efdab92c","_cell_guid":"f6515446-1ebd-4f15-af6c-fd0c68f862f8","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false}},"outputs":[],"execution_count":null}]}